name: pretty_json
description: A Flutter package for pretty printing JSON with key coloring.
version: 0.0.1
homepage: https://github.com/asimsharf/pretty_json.git

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
